# === Django Settings ===
SECRET_KEY=django-insecure-CHANGE-THIS-IN-PRODUCTION-use-python-secrets-token
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# === Database ===
# SQLite (desenvolvimento local)
DATABASE_URL=sqlite:///db.sqlite3

# PostgreSQL (quando migrar para produção)
# DATABASE_URL=postgresql://user:password@localhost:5432/legal_system

# === Email Configuration ===
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

# === API Keys (se necessário) ===
# PJE_API_KEY=
# OTHER_API_KEY=

# === CORS (para frontend) ===
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# === Security (produção) ===
# SECURE_SSL_REDIRECT=True
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True
